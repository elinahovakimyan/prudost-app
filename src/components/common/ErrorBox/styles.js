import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  errorText: {
    padding: 10,
    paddingTop: 30,
    textAlign: 'center',
    color: '#ed3e3e',
  },
});

export default styles;
